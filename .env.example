GROQ_API_KEY=
GEMINI_API_KEY=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_KV_NAMESPACE_ID=
CLOUDFLARE_API_TOKEN=

# Chỉ cần đặt nếu dùng domain riêng (custom domain). Nếu để trống, app sẽ
# tự nhận diện origin qua request header — đủ dùng cho hầu hết trường hợp,
# bao gồm cả Vercel preview URL.
NEXT_PUBLIC_BASE_URL=